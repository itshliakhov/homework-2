function subNumbers() {
  let a = Number(prompt("Write first number you want to subtract"));
  let b = Number(prompt("Write second number you want to subtract"));
  console.log(`Result of subtracting: ${a - b}`);
}
subNumbers();
