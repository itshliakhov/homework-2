function multiplyNumbers() {
  let a = Number(prompt("Write first number you want to multiply"));
  let b = Number(prompt("Write second number you want to multiply"));
  console.log(`Result of multiplication: ${a * b}`);
}
multiplyNumbers();
