function divisionNumbers() {
  let a = Number(prompt("Write first number you want to split"));
  let b = Number(prompt("Write second number that you will divide"));
  console.log(`Result of dividing: ${a / b}`);
}
divisionNumbers();
